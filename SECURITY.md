# 安全策略

## 报告安全漏洞

如果你发现安全问题,**请不要在 public issue 里讨论**。

请通过以下任一方式联系维护者:

- GitHub 私密漏洞报告:在仓库 Security → Advisories → "Report a vulnerability"
- 邮箱:**通过 GitHub Profile 上的联系方式私信**

报告时请尽量包含:

- 受影响的版本(commit SHA / 发布版本号)
- 复现步骤
- 你认为可能的影响范围
- 修复建议(可选)

我们会:

1. 在 **48 小时内** 确认收到
2. 在 **7 天内** 给出影响评估和修复时间表
3. 修复发布后,在 Release Notes / CHANGELOG 中致谢报告者(如同意)

## 支持的版本

| 版本 | 安全修复 |
|---|---|
| latest `main` | ✅ |
| 旧版本 | ❌(请升级到 latest) |

项目还处于早期阶段,只维护 main 分支。

## 已知风险与边界

> ⚠️ Sift 思筛是一个**自部署 SaaS**,不是公网公共服务。开源代码中包含的安全模型假设:
>
> - 部署者控制服务器 + 数据库
> - 用户在受信任的内网或绑定域名 + HTTPS 访问
> - DEEPSEEK_API_KEY 通过环境变量注入,不写入代码
>
> 如果你公开暴露 Sift 实例到互联网,请务必:
>
> - 启用 HTTPS(Certbot + Nginx)
> - 配置反向代理速率限制(项目内已有应用层限流,但反代层是第二道防线)
> - 定期更新依赖:`npm audit fix`
> - 不要让 root 用户跑应用进程(Dockerfile 已用 `node` 用户)

## 致谢

感谢以下安全研究者(暂无):

<!-- 这里会列出报告并已修复的研究者 -->
